/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Adicionar domínios de imagens externas conforme necessário (Fase 3/4)
    formats: ["image/webp"],
  },
};

export default nextConfig;
